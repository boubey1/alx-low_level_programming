#include "main.h"

/*
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
	char letter = 'a';
	
	while (letter < 'z' + 1)
	{
		void print_alphabet(void);
		{
			_putchar(letter);
		}
	letter++;
	}
	if ((letter = 'z'))
	{
		_putchar('\n');
	}
	return (0);
}
