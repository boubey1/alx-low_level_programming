#include "main.h"

/**
 * _abs - compute the absolute value of int
 * @n: the value of the int
 * Return: value of the int, if not return 0
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
