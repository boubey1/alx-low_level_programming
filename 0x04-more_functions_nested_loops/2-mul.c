#include "main.h"

/**
 * mul - it multiples two integers
 * @a: first integer
 * @b: second integer
 * Return: the result of the multiplication
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
